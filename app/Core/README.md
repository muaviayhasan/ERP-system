# Core

Application-wide foundational code that the whole ERP depends on:
base classes, abstract controllers/services, contracts/interfaces,
enums, exceptions, and other cross-cutting infrastructure.

This folder is a placeholder — no business logic yet.
